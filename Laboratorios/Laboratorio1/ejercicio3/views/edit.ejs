<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar</title>
</head>
<body>
    <div class="container">
        <h1>Editar</h1>
        <form action="/edit/<%= contacto.id %>" method="POST">
            <label>Nombres:</label>
            <input type="text" name="nombres" value="<%= contacto.nombres %>" required><br>
            <label>Apellidos:</label>
            <input type="text" name="apellidos" value="<%= contacto.apellidos %>" required><br>
            <label>Dirección:</label>
            <input type="text" name="direccion" value="<%= contacto.direccion %>"><br>
            <label>Teléfono:</label>
            <input type="number" name="telefono" value="<%= contacto.telefono %>"><br>
            <button type="submit">Actualizar</button>
        </form>
        <a href="/">Volver</a>
    </div>

    <style>
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        form {
            margin: 20px 0;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input {
            padding: 5px;
            margin-bottom: 10px;
        }

        button {
            padding: 5px 10px;
            background-color: red;
            color: white;
            border: none;
            border-radius: 3px;
            cursor: pointer;
        }

        button:hover {
            background-color: darkred;
            color: white;
        }

        a {
            text-decoration: none;
            padding: 5px 10px;
            background-color: red;
            color: white;
            border-radius: 3px;
        }

        a:hover {
            background-color: darkred;
            color: white;
        }
    </style>
</body>
</html>
